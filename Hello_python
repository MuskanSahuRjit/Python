# example
x = 10           # integer
y = 3.5          # float
name = "MuskanSahuRjit"    # string
is_active = True # boolean

print(x, y, name, is_active)
print(type(x), type(y), type(name), type(is_active))
